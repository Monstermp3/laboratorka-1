# Лабораторная работа №9

## Тема
Практическая работа с Git

## Цель работы
Ознакомление с системой контроля версий Git, освоение основных команд для работы с локальными и удалёнными репозиториями.

## Выполненные задания

1. **Установка Git** – выполнена установка через `apt-get install git`.
2. **Создание репозитория на GitHub** – создан удалённый репозиторий.
3. **Клонирование репозитория** – выполнен `git clone` на локальную машину.
4. **Создание файла** – создан файл `new_file.txt` в репозитории.
5. **Добавление и коммит** – файл добавлен в индекс (`git add`) и закоммичен (`git commit`).
6. **Работа с удалённым репозиторием** – добавлен удалённый репозиторий (`git remote add`), изменения отправлены (`git push`).
7. **Работа с ветками** – создана новая ветка, отправлена на удалённый сервер и удалена оттуда.
8. **Получение изменений** – выполнена команда `git pull` для обновления локальной копии.

## Вывод
Освоены базовые операции Git: клонирование, коммит, работа с ветками, синхронизация с удалённым репозиторием. Получены практические навыки работы с системой контроля версий.